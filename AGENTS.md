# DAYTRACK 작업 원칙

- 음악은 모든 기록의 중심이다. 앨범 표지는 자르거나 텍스트를 덮지 않는다.
- 스키마 변경은 Prisma migration으로 기록하고, 적용 전후 diff와 테스트를 확인한다.
- Spotify Web API는 작업 시 공식 문서에서 현재 경로를 확인한다. 콘텐츠를 변형하거나 음원을 저장하지 않는다.
- UI는 `docs/DESIGN_SPEC.md`의 종이·먹색·옥스블러드 디자인 토큰과 편집형 여백을 유지한다.
- 변경 후 `pnpm typecheck`, `pnpm test`, `pnpm build`를 실행한다.
- 토큰, 좌표, 일기, 감정, 미디어를 민감 정보로 취급한다. 소유권 검사와 비공개 기본값을 훼손하지 않는다.
